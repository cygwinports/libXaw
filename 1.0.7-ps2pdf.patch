--- origsrc/libXaw-1.0.7/configure.ac	2009-10-17 16:45:19.000000000 -0500
+++ src/libXaw-1.0.7/configure.ac	2009-10-18 17:44:05.566659300 -0500
@@ -23,6 +23,7 @@ AC_PROG_AWK
 AC_PROG_SED
 
 AC_PATH_PROGS([GROFF], [groff], [none], [$PATH:/usr/gnu/bin])
+AC_PATH_PROGS([PS2PDF], [ps2pdf], [none], [$PATH:/usr/gnu/bin])
 
 AC_MSG_CHECKING([whether to build documentation])
 AC_ARG_ENABLE(docs, AC_HELP_STRING([--enable-docs],
@@ -42,6 +43,7 @@ if test "x${build_docs}" = xyes && test 
 fi
 
 AM_CONDITIONAL(BUILD_DOCS, [test x$build_docs = xyes])
+AM_CONDITIONAL(HAVE_PS2PDF, [test x$PS2PDF != xnone])
 
 # Need to call this explicitly since the first call to PKG_CHECK_MODULES
 # is in an if statement, and later calls would break if it's skipped.
--- origsrc/libXaw-1.0.7/spec/Makefile.am	2009-10-16 16:26:55.000000000 -0500
+++ src/libXaw-1.0.7/spec/Makefile.am	2009-10-18 17:47:00.676901000 -0500
@@ -49,13 +49,20 @@ spec_input = $(spec_sources:%=$(srcdir)/
 EXTRA_DIST = $(spec_sources) $(XIDXMACROS) block.awk fixindex.awk widg.idxmac.t
 
 if BUILD_DOCS
-doc_DATA = widgets.ps widgets.index.ps widgets.txt widgets.html
+
+if HAVE_PS2PDF
+printable_format = .pdf
+else
+printable_format = .ps
+endif
+
+doc_DATA = widgets$(printable_format) widgets.index$(printable_format) widgets.txt widgets.html
 
 CLEANFILES = $(doc_DATA)
 MOSTLYCLEANFILES = index.*
 
 GROFF_DEFS = -dxV="$(PACKAGE_STRING)"
-GROFF_FLAGS = -t -ms $(GROFF_DEFS)
+GROFF_FLAGS = -t -ms $(GROFF_DEFS) -I$(srcdir)
 
 widgets.ps: $(spec_input)
 	-$(AM_V_GEN) $(GROFF) -Tps $(GROFF_FLAGS) $(spec_input) \
@@ -63,6 +70,9 @@ widgets.ps: $(spec_input)
 	@if grep '^[^1-9.]' index.raw | grep -v warning; then exit 1; \
 	 else test $$? -le 1; fi
 
+widgets.pdf: widgets.ps
+	$(AM_V_GEN) $(PS2PDF) $< $@
+
 widgets.txt: $(spec_input)
 	$(AM_V_GEN) $(GROFF) -Tascii $(GROFF_FLAGS) $(spec_input) \
 	 2> index.txt.raw > $@
@@ -87,6 +97,8 @@ widgets.index.ps: widg.idxmac.t index.tr
 	$(AM_V_GEN)$(GROFF) -me $(GROFF_DEFS) \
 	 $(srcdir)/widg.idxmac.t $(srcdir)/$(XIDXMACROS) index.troff > $@
 
+widgets.index.pdf: widgets.index.ps
+	$(AM_V_GEN) $(PS2PDF) $< $@
 
 # Useful for running off part of the manual by hand,
 # e.g., make part PART=Label
